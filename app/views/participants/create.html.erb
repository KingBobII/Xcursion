<%= simple_form_for participant do |form| %>
  <%= form.input :username, placeholder: 'Enter the name for your participant' %>
  <%= form.input :email, placeholder: 'Enter the email address for your participant' %>
  <%= form.button :submit %>
<% end %>
